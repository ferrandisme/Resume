{
  "name": "resume",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "firebase:deploy": "npm run build && npx firebase-tools deploy --only hosting",
    "firebase:preview": "npm run build && npx firebase-tools hosting:channel:deploy preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.17.1"
  }
}
